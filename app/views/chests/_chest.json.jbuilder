# json.extract! chest, :id, :classify_alias, :surplus, :description, :created_at, :updated_at
# json.items chest.items do |item|
# 	json.partial! "chest_items/chest_item", chest_item: item
# end
# json.url chest_url(chest, format: :json)