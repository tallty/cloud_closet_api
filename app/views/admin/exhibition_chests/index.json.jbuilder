json.admin_exhibition_chests @exhibition_chests, partial: 'admin/exhibition_chests/admin_exhibition_chest', as: :exhibition_chest
json.user_info(@user.user_info, :nickname, :phone) if @user.try(:user_info)